PORT=4000

# Google Programmable Search JSON API (optional).
# If omitted, the app runs in demo mode with sample leads.
GOOGLE_SEARCH_API_KEY=
GOOGLE_SEARCH_CX=

# SMTP email settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
FROM_NAME=Recruitment Team
